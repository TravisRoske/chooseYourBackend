name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
    Explore-GitHub-Actions:
        runs-on: ubuntu-latest
        steps:
            - run: curl ${{ secrets.MAINSERVERURL }}:${{ secrets.UPDATEPORT }}/update
            - run: curl ${{ secrets.DBSERVERURL }}:${{ secrets.UPDATEPORT }}/update

            - run: echo "🍏 This job's status is ${{ job.status }}."

            # - run: echo "${{ secrets.CLEARFLOWKEYPAIR }}"

            # - run: aws configure set region us-east-2
            # # - run: echo "${{ secrets.CLEARFLOWKEYPAIR }}" | tr -d '\r' > key.pem
            # - run: echo "${{ secrets.CLEARFLOWKEYPAIR }}" > key.pem
            # - run: chmod 600 key.pem
            # - run: cat key.pem
            # - run: file key.pem
            # - run: ssh -tt -o StrictHostKeyChecking=no -i key.pem ec2-user@ec2-3-136-109-163.us-east-2.compute.amazonaws.com
            # - run: echo ls

            # - run: ls

            # - run: endssh
